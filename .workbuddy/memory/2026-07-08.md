# 2026-07-08

## Supabase 保活 GitHub Actions
- 创建 `.github/workflows/keep-alive.yml`，每日 UTC 3:00（北京时间 11:00）通过 curl 调用 Supabase REST API 查询 stories 表，防止项目因流量不足被禁用。
- 需要在 GitHub Secrets 中配置 `SUPABASE_URL` 和 `SUPABASE_ANON_KEY`。
